
<container class="all-page">
  <% if flash[:msg] %>
    <div class="message"><%= flash[:msg] %></div>
  <% end %>

  <h2>Choose a category:</h2>

  <%# <container class="categories"> %>
    <ul>
    <% @rounds.each do |round| %>
      <span class="each-category">
          <li><%= link_to round.name, round_path(round.id) %></li><br>
      </span>
    <% end %>
    </ul>
  <%# </container> %>
</container>






